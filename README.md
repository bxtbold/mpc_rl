# mpc_rl

MPC-RL is a Python library for Model Predictive Control (MPC) and Reinforcement Learning (RL).

## Installation

```bash
git clone https://github.com/bxtbold/mpc_rl.git
cd mpc_rl
pip install -e .
```
